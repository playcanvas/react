import { SplatViewer, FullScreenButton, DownloadButton, MenuButton, Timeline } from "./components/ui/splat-viewer";

export const registryComponents = {
    SplatViewer,
    FullScreenButton,
    DownloadButton,
    MenuButton,
    Timeline,
}