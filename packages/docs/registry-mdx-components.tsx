import { SplatViewer, FullScreenButton, DownloadButton, MenuButton } from "./components/ui/splat-viewer";

export const registryComponents = {
    SplatViewer,
    FullScreenButton,
    DownloadButton,
    MenuButton,
}