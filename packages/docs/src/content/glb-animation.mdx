export const Animation = () => {
    const { data: asset } = useModel('/t-rex.glb');
    return (<Entity>
        {/* Create some environmental lighting. */}
        <EnvAtlasComponent src='/environment-map.png' />
        {/* Render the background grid */}
        <Grid />

        {/* Create a camera entity */}
        <Entity name='camera' position={[4, 2, 4]}>
            <Camera clearColor='#090707' fov={28} />
            <OrbitControls inertiaFactor={0.07} distanceMin={6} distanceMax={10} pitchAngleMin={1} pitchAngleMax={90}/>
            <PostEffects /> {/* Add some Effects */}
            <AutoRotate />
        </Entity>

        <Entity>
            {/* Load our asset */}
            <Render type='asset' asset={asset} >
                <Anim asset={asset} />
            </Render>
        </Entity>
    </Entity>)
}

<Animation />