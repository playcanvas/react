---
title: Render
description: Documentation for the Render component
openGraph:
    title: playcanvas/react docs - Render
    description: Documentation for @playcanvas/react
    images:
        - url: https://playcanvas-react.vercel.app/pc_react-og.png
          width: 1200
          height: 630
---

# Render

## Usage

#### Rendering an Asset

To render a 3D model loaded from a URL, you can use the asset type and pass the asset directly to the `asset` prop.

```jsx copy filename="render-asset.jsx"
import { Render } from '@playcanvas/react/components'
import { useModel } from '@playcanvas/react/hooks'

const RenderModel = () => {
    const { asset } = useModel('model.glb')

    if (!asset) return null

    return <Entity>
        <Render type="asset" asset={asset} />
    </Entity>
}
```

#### Rendering a primitive shape
This will render a simple box primitive transformed by the parent entity.

```jsx copy filename="render-box.jsx"
import { Render } from '@playcanvas/react/components'

const RenderBox = () => {
    return <Entity>
        <Render type="box" />
    </Entity>
}
```

## Props

The following props are based on the [Render Component](https://api.playcanvas.com/engine/classes/RenderComponent.html) from PlayCanvas version **<PcVersion />**. This documentation is automatically generated from the TypeScript types of the installed PlayCanvas package.

<TSDoc
  code={`import type { Render } from '@playcanvas/react/components'
type $ = React.ComponentProps<typeof Render>
export default $`}
/>