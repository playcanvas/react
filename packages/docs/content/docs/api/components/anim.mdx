---
title: Anim
description: Documentation for the Anim component
openGraph:
    title: playcanvas/react docs - Anim
    description: Documentation for @playcanvas/react
    images:
        - url: https://playcanvas-react.vercel.app/pc_react-og.png
          width: 1200
          height: 630
---
# Anim

{/* ## Props

### asset
The asset to use for the animation.

Type: [`Asset`](https://api.playcanvas.com/engine/classes/Asset.html)

### clip
The name of the animation clip to play.

Type: `string`

### loop
Whether to loop the animation.

Type: `boolean`
Default: `false`

### Additional Props

Any additional props will be passed to the PlayCanvas AnimComponent class directly. See the [PlayCanvas documentation](https://api.playcanvas.com/engine/classes/AnimComponent.html) for more information. */}

## Usage

```jsx copy filename="anim.jsx"
import { Entity } from '@playcanvas/react'
import { Anim, Render } from '@playcanvas/react/components'
import { useAsset } from '@playcanvas/react/hooks'

const AnimatingDinosaur = (props) => {

    const { asset } = useAsset('/dinosaur.glb')

    if (!asset) return null

    return (<Entity {...props}>
        <Render type="asset" asset={asset} />
        <Anim asset={asset} clip="Walk" loop />
    </Entity>)
}
```