---
title: Docs
description: A thin react library for for creating 3D apps that supports pointer events, physics, gaussian splats, and more straight out of the box.
openGraph:
    title: playcanvas/react docs
    description: Documentation for @playcanvas/react
    images:
        - url: https://playcanvas-react.vercel.app/pc_react-og.png
          width: 1200
          height: 630
---
import { Cards, Tabs } from 'nextra/components'
import { Icons } from '@docs-components/Icons'
import { Terminal, ArrowRight, Hexagon, Box, Codesandbox } from 'lucide-react'
import Example from '@templates/HomePageExample'

<h1 className='text-4xl font-bold mt-8 mb-2'>
  Playcanvas React
</h1>

<div className='text-lg font-semibold text-muted-foreground'>
  Build interactive 3D experiences with React — without the boilerplate. ⚡
</div>

A powerful, declarative layer over the PlayCanvas engine, making it easy to create dynamic, GPU-accelerated apps with physics, assets, events, and more — all from JSX.

[Get Started](/docs#getting-started)

<div className='w-[calc(100%+4rem)] aspect-video rounded-xl overflow-hidden mt-8 -mx-8'>
  <ReactQueryProvider>
    <Application className='hover:cursor-grab active:cursor-grabbing w-full' fillMode="NONE" resolutionMode="AUTO">
      <Example/>
    </Application>
  </ReactQueryProvider>
</div>

<div className="flex flex-row gap-6 my-4 -mt-12">
  <a className="group shadow flex items-center -ml-2 flex-row gap-2 bg-muted rounded-full p-2 px-4 max-w-fit transition-colors duration-150" href="https://codesandbox.io/p/sandbox/playcanvas-react-nwlrlm" target="_blank">
    <Codesandbox size={14} className="text-muted-foreground group-hover:text-foreground" />
    <div className="text-xs font-medium text-muted-foreground group-hover:text-foreground transition-colors duration-150">Open in Codesandbox</div>
    <ArrowRight size={14} className="text-muted-foreground group-hover:text-foreground transition-colors duration-150" />
  </a>

  <a className="group shadow flex items-center -ml-2 flex-row gap-2 bg-muted rounded-full p-2 px-4 max-w-fit transition-colors duration-150" href="/examples/model-viewer" target="_blank">
    <Box size={14} className="text-muted-foreground group-hover:text-foreground" />
    <div className="text-xs font-medium text-muted-foreground group-hover:text-foreground transition-colors duration-150">Open in Playground</div>
    <ArrowRight size={14} className="text-muted-foreground group-hover:text-foreground transition-colors duration-150" />
  </a>
</div>

## Why?

**@playcanvas/react is designed to get you building fast** — without the usual mess of wiring together separate libraries for physics, input or asset management. It’s a complete, batteries-included toolkit for interactive 3D experiences in React.

It's built around the PlayCanvas engine - a battle-tested, real-time 3D engine that powers some of the most popular apps in the world.

## Getting started

⚡ Start building in minutes with our [Stackblitz template](https://playcanvas-react.vercel.app/new).

Or for a local npm setup run the following and follow the [Getting Started Guide](/docs/guide/getting-started).

```bash copy
npm install @playcanvas/react playcanvas
```

You can also clone the following starter template.

```bash copy
git clone https://github.com/marklundin/playcanvas-react-template.git
```

## How does it work?

Easy - You create an `Application` and add `Entities` and `Components` to it. Entities have position, rotation and scale. `Components` add behaviour to entities.

```jsx copy filename="simple-example.jsx"
import { Application, Entity } from '@playcanvas/react'
import { Camera } from "@playcanvas/react/components"
import { OrbitControls } from "@playcanvas/react/scripts"

const App = () => (
  <Application>
    <Entity position={[0, 0, 4]}>
      <Camera clearColor="blue"/>
      <OrbitControls />
    </Entity>
    <Entity position={[0, 0, 0]}>
      <Render type="cube" />
    </Entity>
  </Application>
);
```

Find additional examples in the [Examples](/examples).

## AI Assisted Development

To get your IDE up to speed, run the following from the root of your project to install the latest MDC rules. Or [grab them here](https://playcanvas-react.vercel.app/rules) to add them manually.

<Tabs items={['Cursor', 'Windsurf']}>
  <Tabs.Tab>
    ```bash copy
    mkdir -p .cursor/rules && curl -s https://playcanvas-react.vercel.app/rules -o .cursor/rules/playcanvas-react.mdc
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```bash copy
    mkdir -p .windsurf/rules && curl -s https://playcanvas-react.vercel.app/rules -o .windsurf/rules/playcanvas-react.mdc
    ```
  </Tabs.Tab>
</Tabs>

## Features

PlayCanvas comes with a number of built in components and scripts that you can use. This makes it easy to get started building your app.

- 🎭 Simple Scene API
- ⏳ Suspenseful Asset loading
- ️👆 Pointer Events
- 🛠️ Physics out of the box
- ⚡ Script component for high frequency updates
- 🏗️ Entity Component System

And lots lots more. Check out the [API Reference](/docs/api/) for more.

## Who's using it?
<Cards>
  <Cards.Card
    icon={<Icons.snapchat />}
    title="Snap"
    href="https://ai.snapchat.com"
  />
</Cards>

If you're using PlayCanvas React, please let us know and we'll add you to the list.

## First steps

If you're familiar with React but new to PlayCanvas and 3D in general, the best place to start is the [Getting Started Guide](/docs/guide/getting-started). It will walk you through creating a simple app and explain the core concepts.

If you're already using PlayCanvas, but new to React, it's worth checking out the [React docs](https://react.dev/learn) first, which provide a great introduction to the React ecosystem. From then you can jump into the [API Reference](/docs/api/entity/).

You can learn more about PlayCanvas on the [developer site](https://developer.playcanvas.com/) and through the [tutorials](https://developer.playcanvas.com/tutorials/). Similarly the React docs has a [great tutorial section](https://react.dev/learn)
