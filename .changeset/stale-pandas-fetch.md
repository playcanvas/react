---
"@playcanvas/react": minor
---

Introduced a new declarative GLTF modification API. Users can now use the `<GltfScene>` and `Modify` components to add, remove, and modify components (like `light`, `render`, `camera`) on entities within a loaded GLB hierarchy.
